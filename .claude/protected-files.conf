# Files protected by the global protect-config hook.
# One pattern per line. Patterns are matched against the file path in the tool input.
# Lines starting with # are comments.

.editorconfig
biome.json
tsconfig.json
vite.config.ts
